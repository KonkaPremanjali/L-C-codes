#include <stdio.h>

int main()
{
    int base,height,area;
    printf("enter base value:");
    scanf("%d",&base);
    printf("enter height value:");
    scanf("%d",&height);
    area=(base*height)/2;
    printf("area of traingle:%d",area);

    return 0;
}